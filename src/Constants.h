#pragma once

const float MAX_SPLINE_SPEED = 0.6F;
const float MIN_SPLINE_SPEED = 0.45F;
const float SPLINE_THICKNESS = 2;
const float SPLINE_DECAY_SPEED = 250;
const int INIT_SPLINES_COUNT = 5000;
const float MAX_DIR_COEF = 1;
const float MAX_CONTROL_DIR_COEF = 1;
const int MAX_DELTA_COEF = 1000;